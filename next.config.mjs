/**
 * Minimal Next.js config (ESM).
 * The previous file contained a `plugins` key that Next.js rejected at runtime.
 * PostCSS/Tailwind is configured in `postcss.config.js` so we don't need extra keys here.
 */
const config = {};

export default config;
